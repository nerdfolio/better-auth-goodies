{
	"name": "ba-guest-list",
	"version": "0.0.1",
	"description": "Similar to anonymous, but with a name that must be on a guest list. Useful for testing or demo with fixed logins",
	"main": "dist/index.cjs",
	"module": "dist/index.mjs",
	"types": "dist/index.d.ts",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "tsup-node"
	},
	"keywords": ["better-auth", "better-auth plugin", "guest login", "demo login"],
	"author": "taivo@github",
	"license": "ISC",
	"packageManager": "pnpm@10.12.1",
	"publishConfig": {
		"access": "public"
	},
	"devDependencies": {
		"@types/lodash-es": "^4.17.12",
		"better-auth": "catalog:"
	},
	"peerDependencies": {
		"better-auth": "catalog:"
	},
	"dependencies": {
		"lodash-es": "catalog:",
		"zod": "catalog:"
	}
}
